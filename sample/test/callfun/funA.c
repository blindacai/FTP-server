#include <stdio.h>
#include <string.h>
#include "funB.h"

int main(){
    char** name;
    name[0] = "apple";
    name[1] = "juice";
    str_len(string);
    printf("length of string: %d\n", str_len(string));
}